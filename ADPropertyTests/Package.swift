// swift-tools-version:5.1

import PackageDescription

let package = Package(
    name: "ADPropertyTests",
    products: [
        .executable(
            name: "ADPropertyTests",
            targets: ["ADPropertyTests"]),
    ],
    targets: [
        .target(
            name: "ADPropertyTests",
            dependencies: []),
    ]
)
